[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["stock_screener"]
markers = [
    "integration: 需要 MySQL 与外网（K 线数据源）",
]

[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
ignore = ["E501"]
